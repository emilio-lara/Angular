// Simple interface
interface Vehicle{
    make: string;
}
// Class using the interface is force to use all the artifact of the interfaces that is implementing
class Car implements Vehicle{
    make:string
}

